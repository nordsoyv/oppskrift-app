h2 {{post.title}}
div {{post.text}}
p
  | Are you sure you want to delete this post? 
  button(ng-click='deletePost()', class="btn-danger") Yes
  |  - 
  button(ng-click='home()', class="btn") No thanks